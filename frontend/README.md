# Frontend

React frontend for the expense tracker.

Important folders:

- `pages`: full screens like Dashboard and Expenses
- `components`: reusable UI pieces
- `services`: Axios API setup
- `routes`: React Router route configuration
- `context`: app-wide state such as logged-in user
- `hooks`: reusable React hooks

Start command:

```bash
npm install
npm run dev
```
